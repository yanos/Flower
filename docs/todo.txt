long term:
 improve cli
 album mode
 artist / playlist icon instead of album art
 lyric service
 liked song tab
 artist image service
 most popular track for an artist service
 localisation
 intelligent playlist like favorite songs, recently played, etc.
 export playlist with actual songs
 on each startup, choose an flower emoji and make that the app icon. show it in the about box and in the ios splash screen. different flower species in  dark mode
 push library sync events instead of polling
 account for family and friends, can only read the library, no playlist, update play count etc.
 ui customization
 better currently playing controls that looks like an lcd panel
 a way to only see downloaded music
 queue after this song or this album
 pulling down the view in mobile should reveal a filter field
 visualizers
 themes
 back / forward arrows
 UI tests

build:
 dotnet target doesnt check the provisioning profile at the correct place, needs to be copied over.
 script to make the provisioning profile.
 more tests
 ui tests

details:
 open log folder in log view
 add an issue link in about box
 readme
 folder in playlist
 view logs from devices from the device section of the sidebar
 landscape mode on mobile needs polishing
 download spinner still not centred on mobile
 server status for client
 when reaching the end of an album, the queued song is the first song from this album
 lock screen widget should stay with the queue song
 album art top align on mobile
 next song doesn't always play
 track last played for each song
 option to toggle each log category (level, category, timestamp)

server / client overhaul:
 on desktop sidebar: checkmark on devices that are paired.
 what happens to the library if the client cancel the pairing
 
 SubSonic vs our own endpoints
 Event bus?
 
Question for Claude:
  album_artist/artist_id/album_id stored, not computed: will modifying a track metadata cause problem?
   if stable-across-rename album identity ever matters, the fix isn't un-storing these — it's a real albums table with its own Guid, which SubsonicIdentity's comment already anticipates. Not needed now.
  
  * Couldnt the server uses the same queries as the client, but then route the results into a paginator to split them into pages for its own purpose (maybe i misunderstood why they are not using the same)
  * Starred track will eventually my available for both server and client. 