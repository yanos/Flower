<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" android:installLocation="auto">
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
	<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
	<uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
	<uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
	<!-- networkSecurityConfig, not usesCleartextTraffic: the flag is one bit
	     and the file can be read, so the reasoning for permitting cleartext at
	     all lives next to the permission. See Resources/xml/network_security_config.xml. -->
	<application android:label="Flower" android:icon="@drawable/Icon"
	             android:networkSecurityConfig="@xml/network_security_config" />
</manifest>
