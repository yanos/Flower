# Caddy gets and renews the certificate, redirects HTTP to HTTPS, and proxies to
# Flower on loopback. FLOWER_HOSTNAME comes from docker-compose.caddy.yml.

{$FLOWER_HOSTNAME} {
	reverse_proxy localhost:4533
}
